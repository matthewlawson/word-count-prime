const mapReduce = require ('../map_reduce/map_reduce')
const expect = require('expect.js');
/**
 * Tests will be exactly the same as brute_force_test to ensure both algorithms produce the same output.
 */
describe('MapReduce() tests', function() {
    //TODO
});
